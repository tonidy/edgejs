'use strict';
const ex = require('example');
if (!ex || !ex.fromNodeModules) {
  console.error('expected require("example") to return { fromNodeModules: true }', ex);
  process.exit(1);
}
console.log('node_modules require test passed');
